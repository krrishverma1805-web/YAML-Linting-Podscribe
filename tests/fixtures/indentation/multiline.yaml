apiVersion: v1
kind: ConfigMap
metadata:
  name: multiline-config
data:
  config.yaml: |
    server {
        listen 80;
        server_name example.com;
    }
  script.sh: >
    echo "Hello World"
    echo "This is a long command"
